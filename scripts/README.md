# Solar Data Comparison Dashboard

This Streamlit app allows you to interactively explore solar energy metrics (GHI, DNI, DHI) across Benin, Sierra Leone, and Togo.

##  Setup Instructions

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
